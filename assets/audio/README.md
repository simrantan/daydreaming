# Audio assets

Add local sample audio tracks here (e.g. `track1.mp3`, `track2.mp3`) for the song bar and queue.

The app currently uses remote placeholder URLs so it runs without local files. Once you add files here, you can switch the daydream service to use `require('@/assets/audio/track1.mp3')` etc.
