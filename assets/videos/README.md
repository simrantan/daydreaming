# Video assets

Add local sample videos here (e.g. `sample1.mp4`, `sample2.mp4`) for the Explore feed.

The app currently uses remote placeholder URLs so it runs without local files. Once you add files here, you can switch the daydream service to use `require('@/assets/videos/sample1.mp4')` etc.
